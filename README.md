## language-tw-loader


使用方法

```
npm i --save-dev language-tw-loader
```


vue

```
{
    test: /\.(js|vue)$/,
    loader: 'language-tw-loader',
}
```


react

```
{
    test: /\.(js|jsx)$/,
    loader: 'language-tw-loader',
}
```
